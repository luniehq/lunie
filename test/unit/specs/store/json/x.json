#**
Make an arbitrary HTTP call when this field is listed in a query selection set.
The "relativePath" is the resource path relative to the root URL provided when you
created the HTTP data source. Pass a "params" object to forward headers, body,
and query parameters to the HTTP endpoint.
*#
{
  "version": "2018-05-29",
  "method": "GET",
  ## E.G. if full path is https: //api.xxxxxxxxx.com/posts then resourcePath would be /posts ** "resourcePath": "/staking/validators",
  "params": {
    "query":$util.toJson($ctx.args),
    "headers": {
      "Authorization": "$ctx.request.headers.Authorization"
    }
  },
  "key": {
    "description__moniker": "${context.data.description.moniker}",
    "description__identity": "${context.data.description.identity}",
    "description__website": "${context.data.description.website}",
    "description__details": "${context.data.description.details}",
    "commission__rate": "${context.data.commission.details}",
    "commission__max_rate": "${context.data.commission.rate}",
    "commission__max_change_rate": "${context.data.commission.max_rate}",
    "commission__update_time": "${context.data.commission.update_time}",
  }
}